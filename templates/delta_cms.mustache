


<!- mod_learningtimecheck/delta_cms -->
<div class="deltas">
    {{#canseeother}}
    <div class="delta-caption">
        <form action="{{{formurl}}}" id="delta-user-form">
        <input type="hidden" name="sesskey" value="{{sesskey}}">
        <input type="hidden" name="id" value="{{cmid}}">
        {{{userselect}}}
        </form>
    </div>
    {{/canseeother}}

    <div class="delta-caption">
        {{#str}}deltas, learningtimecheck{{/str}}
    </div>

    <div class="delta-cms">
        {{#cms}}
        {{> mod_learningtimecheck/delta_cm }}
        {{/cms}}
    </div>
</div>
<!- /mod_learningtimecheck/delta_cms -->